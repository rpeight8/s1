/**
 * Determines if a value is truthy in JavaScript
 * @param {*} value - Any JavaScript value to check for truthiness
 * @returns {boolean} - Returns true if the value is truthy, false otherwise
 * @example
 * isTruthy(1)        // true
 * isTruthy("")       // false
 * isTruthy(null)     // false
 * isTruthy([])       // true
 */
function isTruthy(value) {
  return false;
}

module.exports = { isTruthy };
